const hamIcon = document.querySelector(".wrapper-hamburger");

hamIcon.onclick = () => hamIcon.classList.toggle("active");
hamIcon.addEventListener("click", () => {
    if (hamIcon.classList.contains("active")) {

    }
})